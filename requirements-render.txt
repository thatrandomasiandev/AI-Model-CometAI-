# Optimized requirements for Render deployment
# Core ML libraries - using CPU-only versions for free tier
torch>=2.0.0+cpu --index-url https://download.pytorch.org/whl/cpu
transformers>=4.30.0
tokenizers>=0.13.0
accelerate>=0.20.0

# Quantization support (optional for free tier)
# bitsandbytes>=0.39.0  # Commented out for CPU-only deployment

# Configuration and utilities
pyyaml>=6.0
numpy>=1.21.0
tqdm>=4.64.0
requests>=2.28.0

# Web server (required)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=11.0.0

# System monitoring
psutil>=5.9.0

# Pydantic for API models
pydantic>=2.0.0
